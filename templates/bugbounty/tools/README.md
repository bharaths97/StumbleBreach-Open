# Tools

One-time environment setup notes for this engagement — one file per
box/environment if more than one is involved (e.g. `mac-setup.md`,
`windows-setup.md`), or a single `setup.md` if it's just one machine.

Cover, at minimum:
- Monitoring/baseline setup to do *before* installing or touching the
  target, if applicable (registry/filesystem baselines, process
  monitors).
- Required tools and how to install them.
- Any file-transfer setup if the testing spans more than one machine.

This file is a placeholder — replace it with the real setup notes once
the engagement's environment is known. Not auto-filled by the planner.
