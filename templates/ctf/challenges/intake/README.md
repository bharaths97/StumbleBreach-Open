# Intake

Drop the raw challenge description files here — one per challenge,
named to match the challenge folder (e.g. `web-01.md`). Include whatever
the competition gave you: challenge name, context, target details,
provided files/links, point value, category.

These files are read-only reference material once dropped in — sessions
read from here but don't edit these.
